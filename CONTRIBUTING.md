## 🤝 Contributing

Contributions are welcome — especially CSS styling, and front-end improvements!  

To contribute:
1. **Fork** this repository  
2. **Clone* your fork  
   ```
   git clone https://github.com/yourusername/heartbeat-server.git
   ```
3. Create a new **branch** for your feature or fix
    ```
   git checkout -b feature/my-improvement
    ```
4. Make your changes, commit, and push
    ```
    git add .
    git commit -m "Feature: add heartbeat shape pulse animation"
    git push origin feature/my-improvement
   ```
5. Open a **ull Request (PR)** describing your update

No contribution is too small — from code cleanup to CSS tweaks, all help keep this heart beating ❤️
